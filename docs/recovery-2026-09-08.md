# Homepage and supplier programme recovery — 8 September 2026

The approved work was present in c01f33b. Merge 858541f subsequently replaced the homepage composition and supplier form refinements with the older production versions. Vercel successfully deployed that merged source, so the problem was the deployed content, not a failed push or stale browser cache.

This recovery restores the final approved homepage layout, wording, mobile workflow, typography hierarchy, spacing, roadmap and full-height centred manifesto. It reconnects the homepage CSS and animation modules and adds them to the production asset inventory. The supplier form regains its mobile header arrangement, space above the footer controls, and prism rendering limits/lifecycle. Newer form validation, focus management, reflow rules, optimized media, WOFF2 fonts, routing and security controls remain in place.

Verification before deployment:
- Production build: 103 HTML routes; all static destinations and assets resolve.
- Node suite: 174 tests pass, including a new built-homepage regression test and restored prism lifecycle coverage.
- Browser homepage: no horizontal page overflow at 320, 360, 390, 430, 768, 1023, 1280 and 1440px. Mobile roadmap summaries appear below the desktop breakpoint and remain hidden on desktop.
- At 390 × 844: menu right inset 12px; programme title 29.25px; manifesto height 844px.
- Supplier application at 390px: header gap 15.6px, title visible, 48px content-to-footer padding, no prism CSS filter. Language selection and Continue work without submitting an application.
- CI now builds before running tests that inspect generated pages, matching the Vercel build order.

No claim of physical-device animation profiling is made. Unrelated local tracker and strategic-insights edits are excluded from this recovery commit.
